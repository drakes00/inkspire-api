# Inkspire

### How to use it ?
In this repo, can be found :
- the Symfony API (to link the front with DB)
- the Ollama API (call the llm) 

To launch it, use the `symfony composer install` and then `symfony serve`.
framework:
    rate_limiter:
        llm_generate:
            policy: sliding_window
            limit: '%app.rate_limiter.llm_limit%'
            interval: '%app.rate_limiter.llm_interval%'

when@test:
    framework:
        rate_limiter:
            llm_generate:
                policy: no_limit
